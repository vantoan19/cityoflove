# Bunny — Pose 5: Amused / Hands on Hips (sprite loop)

## Source image (required, upload for every frame)

- **Bunny base (identity anchor):** `C:\Projects\zootopia\assets\basee.png`

Identity from `basee.png`. The amused pose is described in the frame 1 prompt. **Every frame must look essentially IDENTICAL to frame 1 with only a tiny sprite delta.**

## Animation description

Sprite loop of an "I can't believe this guy" amused smile. 5 frames, very gentle deltas, cycle 1 → 2 → 3 → 4 → 5 → 1.

## Sprite-animation rules

- Identity + outfit + palette + framing + scale + background from `basee.png` — same across all frames.
- Per-frame deltas are **tiny** — 1–2 pixel hip/head shifts or sub-degree tilts.
- Do not re-pose the bunny.

---

## Frame 1 — Amused baseline

```text
Use the uploaded bunny base image (identity anchor). Create a full-body image of the same bunny character with identity exactly from the base, on a clean white background, in the same crayon style and same framing, scale, and proportions as the base. Pose: hands on hips (as in the base) with these small expression changes — weight even on both legs, both ears tilted slightly forward (engaged), eyes slightly narrowed in a genuine laugh-smile (corners crinkle), closed-mouth grin with both corners raised, head level. Preserve white fur with pink inner ears, blue police uniform, orange safety vest, blue police hat with badge. Keep all white fur fully opaque.
```

## Frame 2 — Left hip +1px out

```text
Use the uploaded bunny base image. Recreate the EXACT same composition as frame 1 of the amused sprite loop. Apply ONLY this micro-change: the left hip pops out about 1 pixel (weight beginning to shift). Head, ears, smile, outfit, background IDENTICAL. Preserve opaque white fur. Do not re-pose the bunny.
```

## Frame 3 — Left hip +2px, head tilt +1° to bunny's right

```text
Use the uploaded bunny base image. Recreate the EXACT same composition as frame 1 of the amused sprite loop. Apply ONLY these micro-changes: the left hip is about 2 pixels out, and the head tilts about 1 degree to the bunny's right (a tiny head-shake). Ears, smile, outfit, background IDENTICAL. Preserve opaque white fur. Do not re-pose the bunny.
```

## Frame 4 — Left hip +1px, head level

```text
Use the uploaded bunny base image. Recreate the EXACT same composition as frame 1 of the amused sprite loop. Apply ONLY this micro-change: the left hip is about 1 pixel out (settling from the peak), head is level. Ears, smile, outfit, background IDENTICAL. Preserve opaque white fur. Do not re-pose the bunny.
```

## Frame 5 — Head tilt +1° to bunny's left

```text
Use the uploaded bunny base image. Recreate the EXACT same composition as frame 1 of the amused sprite loop. Apply ONLY this micro-change: the head tilts about 1 degree to the bunny's left (opposite side from frame 3 — a tiny continued head-shake). Hips level, ears, smile, outfit, background IDENTICAL. Preserve opaque white fur. Do not re-pose the bunny.
```

---

## Save frames as

- `frame1.png` … `frame5.png`
